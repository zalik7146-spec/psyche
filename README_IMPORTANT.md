# ⚠️ ВАЖНО: Добавьте index.css

Файл `src/index.css` не включён в архив (он был предоставлен в чате как текст, не как файл).

Возьмите ваш оригинальный `src/index.css` и положите его в `src/index.css`.

Все остальные файлы актуальны и содержат последние исправления.

## Что изменилось в этой версии:
- **BookCatalog.tsx** — две вкладки: «Читать онлайн» (Gutendex, реальные тексты) + «Каталог» (Google Books)
- **ReaderView.tsx** — полностью переписан: иммерсивный режим, исправлена изоляция шрифта, выделения, закладки
- **MessagesView.tsx** — Telegram-стиль: пузыри, реакции, ответы, emoji-панель
- **ProfileView.tsx** — убрана вкладка «Инструменты», добавлен горизонтальный стрип
- **index.html** — исправлен CSP (добавлены все нужные домены)
- **vercel.json** — исправлен manifest 401 (убраны Vercel заголовки кеширования)
